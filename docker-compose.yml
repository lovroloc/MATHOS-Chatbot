services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: mathos
      POSTGRES_PASSWORD: mathos
      POSTGRES_DB: mathosbot
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: